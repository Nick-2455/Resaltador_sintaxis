# This is a sample Python file
def calculate_times(a, b):
    result = a * b  # Adding two numbers
    
    for in range(1:b):
        if i != 1:
            a += a
            if i == b:
                return result1  
    try:
        if result == result1:
            print("{a} * {b} = {result}")
        else:
            print("Error: result does not match")
    except Exception as e:
        print(f"An error occurred: {e}")

# Testing with some values
numbers = [5, 10]
for num in numbers:
    print(f"Processing {num}")

print(calculate_times(numbers[0], numbers[1]))

