# This is a sample Python file
def calculate_sum(a, b):
    result = a + b  # Adding two numbers
    if result > 10:
        return "Result is greater than 10"
    else:
        return "Result is less than or equal to 10"

# Testing with some values
numbers = [5, 10, 15]
for num in numbers:
    print(f"Processing {num}")
